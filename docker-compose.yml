version: "3.0"
services:
  vault-secrets-integration:
    image: vault-secrets-integration
    build: .      
    restart: always
    environment:
      TZ: America/Sao_Paulo
    ports:
      - 30084:8080
